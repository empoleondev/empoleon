import type { HighlightFactory } from '@empoleon/core';
import type { StylesApiData } from '../types';

export const HighlightStylesApi: StylesApiData<HighlightFactory> = {
  selectors: {
    root: 'Root element',
  },

  vars: {},
};
