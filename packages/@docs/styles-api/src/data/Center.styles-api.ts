import type { CenterFactory } from '@empoleon/core';
import type { StylesApiData } from '../types';

export const CenterStylesApi: StylesApiData<CenterFactory> = {
  selectors: {
    root: 'Root element',
  },

  vars: {},
};
